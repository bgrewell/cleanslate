[Unit]
# Wait for cleanslate to populate /etc/ssh/host_keys/ before sshd tries to load
# them. Without this, ssh.service can be socket-activated before the keys
# exist and fail-fast itself out.
Wants=cleanslate-host-keys.service
After=cleanslate-host-keys.service
